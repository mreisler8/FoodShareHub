A) Auth Hotfix (Surgical) — Password Hashing + Minimal Tests

Goal: make login work so we can validate Lists end-to-end. Keep it tiny and fully tested.

Prompt to run (implementation allowed):

Title: Auth Hotfix — Password Hashing & Session (Surgical, Fully Tested)

Mode: IMPLEMENT. Keep changes minimal and isolated. No unrelated edits.

Scope & Rules

Back end only: auth routes/services + one supertest file.

Use bcrypt (or bcryptjs) for hashing and comparison.

Registration: hash on write.

Login: compare hash with bcrypt.compare.

Do not alter user schema beyond adding a hash if missing.

Do not change session strategy—only ensure successful login returns 200 and sets session/cookie.

Acceptance tests (supertest)

POST /api/auth/register → 201 on new user; password not echoed; DB stores hashed password.

POST /api/auth/login (correct creds) → 200; sets auth cookie; GET /api/me returns user.

POST /api/auth/login (wrong creds) → 401.

Password re-hash avoidance: registering the same email twice → 409 (or your existing behavior).

Test names: auth.spec.ts (or match your repo’s test layout).

Deliverables

Minimal code changes in auth controller/service.

tests/auth.spec.ts with the above scenarios.

Short note in docs/AUTH_FIX_NOTES.md (what changed, how to roll back).

B) Query Keys Consistency Sweep (Mechanical)

Goal: finish the hierarchical key consolidation the agent started so cache invalidation is reliable.

Prompt to run (implementation allowed):

Title: Lists — Query Key Centralization (Mechanical sweep + zero behavior change)

Mode: IMPLEMENT. Mechanical refactor + local verification. No behavior changes.

What to do

Replace all string endpoint keys with hierarchical arrays:

['lists']

['lists','user', userId]

['lists', listId]

['lists', listId, 'items']

['saved-lists']

['saved-lists', listId]

Ensure every mutation (create list, edit, add item, reorder, save/unsave) invalidates the correct keys using small helpers:

invalidateList(listId)

invalidateCollections(userId?)

invalidateSaveStatus(listId)

Do not rename components or move files. Keep diff tight.

Spot checks to include

Save button on list tile and list details both reflect correct state after save/unsave without full page reload.

Navigation to another page and back shows persisted state (cache + server are aligned).

Deliverables

PR summary with a file list and before/after key examples.

docs/lists-validation/QUERY_KEYS_CHANGELOG.md (table of “was → now” for each component).

C) Re-run a strict, read-only validation

Now that auth works and query keys are consistent, validate lists end-to-end with evidence only.

Prompt to run (read-only only):

Title: Lists MVP — Read-Only Final Validation (Evidence-Backed, No Code Changes)

Mode: READ-ONLY. Do not change code or DB.

What to validate (and attach evidence):

Auth & Personas

Log in as User A (owner), B (follower of A), C (member of circle C1), D (no relation).

Capture GET /api/me for each (200 with identity).

Visibility_v2 matrix (policy vs reality)

For one list of each visibility (public, private, followers, circle):

Capture GET /api/lists/:id + .../items responses and HTTP status for A/B/C/D.

Confirm only visibility_v2 (and circle membership when circle) governs access—legacy booleans not relied on (cite response/log snippets).

Save/Unsave coherence

Observe (read-only) a save/unsave cycle you or QA triggers:

GET /api/lists/:id/save-status returns {saved:boolean} and UI matches on tiles and details.

Verify no component calls /api/saved-lists to infer single-list status.

Note which query keys refetch/invalidate (from dev logs).

Add items & reorder (observed)

Add the same restaurant twice → no duplicate (idempotent).

Reorder → persists after refresh.

Attach request/response snippets.

Pagination

GET /api/lists / GET /api/saved-lists / GET /api/lists/:id/items:

Show ?limit=&cursor= behavior (or document absence).

If present, show two-page retrieval with stable nextCursor.

Accessibility

Save button: aria-pressed toggles; aria-label is descriptive.

Reorder: focus ring + SR live region message on move (or document gap).

Include axe (or similar) violations list (violations only).

Perf & cache

Capture P95 from performance logs for:

GET /api/lists/:id

GET /api/lists/:id/items

GET /api/lists/:id/save-status

Note any duplicate requests on first render.

Output files

docs/lists-validation/CRITICAL_VALIDATION_REPORT.md (Go / Conditional Go / No-Go with explicit blockers and ETA)

docs/lists-validation/VISIBILITY_MATRIX.md

docs/lists-validation/QUERY_KEYS_AUDIT.md

docs/lists-validation/ACCESSIBILITY_SCAN.md

Screenshots → docs/lists-validation/screens/ (link them from the report)