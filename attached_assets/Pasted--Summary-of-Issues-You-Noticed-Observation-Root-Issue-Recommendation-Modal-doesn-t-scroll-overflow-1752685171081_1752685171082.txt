🧭 Summary of Issues You Noticed
Observation	Root Issue	Recommendation
Modal doesn’t scroll	overflow-hidden or modal max-height not set	Apply overflow-y-auto and set max-h-screen on modal content
Double “X” close icons	Component nesting causes duplicate headers	Remove X from child content or restructure header slot
Clicking outside modal → wrong redirect	Missing cancel handler or default route	Override overlay click to call onClose() not route push
Food Moment = photo required	Too rigid for quick posting	Relax validation — allow photo OR caption
Feature overkill per tag	All forms have full fields	Streamline fields based on intent (see below)

✅ Revised UX Strategy Per Post Type
🍽️ 1. Food Moment
Goal: Quick snapshot of what you're eating now

Current Required: Photo (❌)
Recommended:

Image (optional)

Caption

Tags (optional)

Share Destination

✅ Let user post a quick note even without a photo
👉 Use Instagram Story / BeReal as mental model

📝 2. Dish Review
Goal: Thoughtful opinion on a specific dish

Fields:

Dish name (required)

Rating (1–5 stars)

What I liked (optional)

What I didn't like (optional)

Image (optional)

Restaurant

Tags

Share Destination

✅ This is the deepest form, okay to keep more fields

📍 3. Restaurant Rec
Goal: Shoutout a restaurant

Fields:

Restaurant name (required)

Type / cuisine (optional)

City (optional)

Tags

Notes / why you recommend it

Image (optional)

Save to List toggle

Share Destination

✅ Tag to Circle/List should be more optional

🧩 UX/UI Fix Recommendations (Dev-Ready)
🔧 1. Modal Improvements
Update modal container:

tsx
Copy
Edit
<div className="bg-white rounded-lg p-4 max-h-screen overflow-y-auto shadow-lg">
Ensure DialogOverlay or Sheet component:

Traps focus

Dismisses with onClose() handler

Does not redirect unless explicitly told

❌ 2. Remove Duplicate “X” Buttons
If you're using Dialog + internal headers:

Add a showClose prop and render <X> in only one place

E.g., move close button to a unified ModalHeader.tsx if reusable

✅ 3. Relax Validation for Food Moment
tsx
Copy
Edit
if (!photo && !caption) {
  showToast("Add a quick note or photo before sharing.");
  return;
}
Allow post as long as one content type exists.

📥 4. Add Preview Toggle (Optional)
Let users click “Preview Post” before sharing:

tsx
Copy
Edit
<Button onClick={() => setShowPreview(true)}>Preview</Button>
{showPreview && <PostPreview {...formData} />}
✅ 5. Clarify Cancel Behavior
When modal closes:

tsx
Copy
Edit
onClose={() => router.push("/home")} // or return to previous state
Avoid default route to /feed unless explicitly intended.

✍️ Next Steps I Can Help With
 Generate updated CreateFoodMoment.tsx, CreateDishPost.tsx, and CreateRestaurantRec.tsx with these clean field structures

 Fix your modal wrapper code for scrolling, single close button, and cancel behavior

 Add preview toggle with matching feed post layout

